services:
  web:
    image: ${QC_DASHBOARD_IMAGE}
    container_name: qianchuan-web
    restart: unless-stopped
    env_file:
      - .env.dashboard
    environment:
      CONFIG_PATH: /app/config/config.json
      DATA_DIR: /app/data
      DATABASE_PATH: /app/data/dashboard.db
      TOKEN_CACHE_PATH: /app/data/token_cache.json
      LATEST_TOKEN_PATH: /app/data/qianchuan_latest_token.json
      UVICORN_WORKERS: ${UVICORN_WORKERS:-2}
      DATABASE_URL: postgresql://${POSTGRES_USER:-qianchuan}:${POSTGRES_PASSWORD:-qianchuan}@postgres:5432/${POSTGRES_DB:-qianchuan}
      REDIS_URL: redis://redis:6379/0
      CELERY_BROKER_URL: redis://redis:6379/1
      CELERY_RESULT_BACKEND: redis://redis:6379/2
      NIGHTLY_HISTORY_WORKERS: ${NIGHTLY_HISTORY_WORKERS:-6}
      NIGHTLY_HISTORY_PLAN_MATERIAL_REQUESTS_PER_MINUTE: ${NIGHTLY_HISTORY_PLAN_MATERIAL_REQUESTS_PER_MINUTE:-300}
      NIGHTLY_HISTORY_PLAN_MATERIAL_BATCH_SIZE: ${NIGHTLY_HISTORY_PLAN_MATERIAL_BATCH_SIZE:-0}
      NIGHTLY_HISTORY_PLAN_MATERIAL_BATCH_SLEEP_SECONDS: ${NIGHTLY_HISTORY_PLAN_MATERIAL_BATCH_SLEEP_SECONDS:-0}
      ENABLE_HOT_SYNC_SCHEDULES: ${ENABLE_HOT_SYNC_SCHEDULES:-1}
      ENABLE_IN_PROCESS_SCHEDULER: "0"
    ports:
      - "9898:9898"
    volumes:
      - ./data:/app/data
    depends_on:
      - postgres
      - redis

  worker:
    image: ${QC_DASHBOARD_IMAGE}
    container_name: qianchuan-worker
    restart: unless-stopped
    command: celery -A dashboard.celery_app.celery_app worker -l info -Q celery
    env_file:
      - .env.dashboard
    environment:
      CONFIG_PATH: /app/config/config.json
      DATA_DIR: /app/data
      DATABASE_PATH: /app/data/dashboard.db
      TOKEN_CACHE_PATH: /app/data/token_cache.json
      LATEST_TOKEN_PATH: /app/data/qianchuan_latest_token.json
      DATABASE_URL: postgresql://${POSTGRES_USER:-qianchuan}:${POSTGRES_PASSWORD:-qianchuan}@postgres:5432/${POSTGRES_DB:-qianchuan}
      REDIS_URL: redis://redis:6379/0
      CELERY_BROKER_URL: redis://redis:6379/1
      CELERY_RESULT_BACKEND: redis://redis:6379/2
      NIGHTLY_HISTORY_WORKERS: ${NIGHTLY_HISTORY_WORKERS:-6}
      NIGHTLY_HISTORY_PLAN_MATERIAL_REQUESTS_PER_MINUTE: ${NIGHTLY_HISTORY_PLAN_MATERIAL_REQUESTS_PER_MINUTE:-300}
      NIGHTLY_HISTORY_PLAN_MATERIAL_BATCH_SIZE: ${NIGHTLY_HISTORY_PLAN_MATERIAL_BATCH_SIZE:-0}
      NIGHTLY_HISTORY_PLAN_MATERIAL_BATCH_SLEEP_SECONDS: ${NIGHTLY_HISTORY_PLAN_MATERIAL_BATCH_SLEEP_SECONDS:-0}
      ENABLE_HOT_SYNC_SCHEDULES: ${ENABLE_HOT_SYNC_SCHEDULES:-1}
    volumes:
      - ./data:/app/data
    depends_on:
      - postgres
      - redis

  history-worker:
    image: ${QC_DASHBOARD_IMAGE}
    container_name: qianchuan-history-worker
    restart: unless-stopped
    command: celery -A dashboard.celery_app.celery_app worker -l info -Q history --concurrency=${NIGHTLY_HISTORY_WORKERS:-6}
    env_file:
      - .env.dashboard
    environment:
      CONFIG_PATH: /app/config/config.json
      DATA_DIR: /app/data
      DATABASE_PATH: /app/data/dashboard.db
      TOKEN_CACHE_PATH: /app/data/token_cache.json
      LATEST_TOKEN_PATH: /app/data/qianchuan_latest_token.json
      DATABASE_URL: postgresql://${POSTGRES_USER:-qianchuan}:${POSTGRES_PASSWORD:-qianchuan}@postgres:5432/${POSTGRES_DB:-qianchuan}
      REDIS_URL: redis://redis:6379/0
      CELERY_BROKER_URL: redis://redis:6379/1
      CELERY_RESULT_BACKEND: redis://redis:6379/2
      NIGHTLY_HISTORY_WORKERS: ${NIGHTLY_HISTORY_WORKERS:-6}
      NIGHTLY_HISTORY_PLAN_MATERIAL_REQUESTS_PER_MINUTE: ${NIGHTLY_HISTORY_PLAN_MATERIAL_REQUESTS_PER_MINUTE:-300}
      NIGHTLY_HISTORY_PLAN_MATERIAL_BATCH_SIZE: ${NIGHTLY_HISTORY_PLAN_MATERIAL_BATCH_SIZE:-0}
      NIGHTLY_HISTORY_PLAN_MATERIAL_BATCH_SLEEP_SECONDS: ${NIGHTLY_HISTORY_PLAN_MATERIAL_BATCH_SLEEP_SECONDS:-0}
      ENABLE_HOT_SYNC_SCHEDULES: ${ENABLE_HOT_SYNC_SCHEDULES:-1}
    volumes:
      - ./data:/app/data
    depends_on:
      - postgres
      - redis

  scheduler:
    image: ${QC_DASHBOARD_IMAGE}
    container_name: qianchuan-scheduler
    restart: unless-stopped
    command: celery -A dashboard.celery_app.celery_app beat -l info
    env_file:
      - .env.dashboard
    environment:
      CONFIG_PATH: /app/config/config.json
      DATA_DIR: /app/data
      DATABASE_PATH: /app/data/dashboard.db
      TOKEN_CACHE_PATH: /app/data/token_cache.json
      LATEST_TOKEN_PATH: /app/data/qianchuan_latest_token.json
      DATABASE_URL: postgresql://${POSTGRES_USER:-qianchuan}:${POSTGRES_PASSWORD:-qianchuan}@postgres:5432/${POSTGRES_DB:-qianchuan}
      REDIS_URL: redis://redis:6379/0
      CELERY_BROKER_URL: redis://redis:6379/1
      CELERY_RESULT_BACKEND: redis://redis:6379/2
      NIGHTLY_HISTORY_WORKERS: ${NIGHTLY_HISTORY_WORKERS:-6}
      NIGHTLY_HISTORY_PLAN_MATERIAL_REQUESTS_PER_MINUTE: ${NIGHTLY_HISTORY_PLAN_MATERIAL_REQUESTS_PER_MINUTE:-300}
      NIGHTLY_HISTORY_PLAN_MATERIAL_BATCH_SIZE: ${NIGHTLY_HISTORY_PLAN_MATERIAL_BATCH_SIZE:-0}
      NIGHTLY_HISTORY_PLAN_MATERIAL_BATCH_SLEEP_SECONDS: ${NIGHTLY_HISTORY_PLAN_MATERIAL_BATCH_SLEEP_SECONDS:-0}
      ENABLE_HOT_SYNC_SCHEDULES: ${ENABLE_HOT_SYNC_SCHEDULES:-1}
    volumes:
      - ./data:/app/data
    depends_on:
      - postgres
      - redis

  postgres:
    image: postgres:17
    container_name: qianchuan-postgres
    restart: unless-stopped
    env_file:
      - .env.dashboard
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-qianchuan}
      POSTGRES_USER: ${POSTGRES_USER:-qianchuan}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-qianchuan}
    volumes:
      - ./volumes/postgres:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: qianchuan-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - ./volumes/redis:/data
